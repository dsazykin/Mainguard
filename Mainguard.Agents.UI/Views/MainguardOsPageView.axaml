<UserControl xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:Mainguard.Agents.UI.ViewModels"
        x:Class="Mainguard.Agents.UI.Views.MainguardOsPageView"
        x:DataType="vm:MainguardOsPageViewModel">
    <StackPanel Spacing="24" MaxWidth="760">

        <!-- Rebuild sandbox images (Tools → Rebuild sandbox images…): no dialog of its own, so it
             lives here rather than getting its own sidebar row. -->
        <Border Classes="Card" Background="{DynamicResource SurfaceCard}" CornerRadius="8" Padding="16">
            <Grid ColumnDefinitions="*,Auto">
                <StackPanel Grid.Column="0" Spacing="4" Margin="0,0,10,0">
                    <TextBlock Text="Rebuild sandbox images" FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource TextPrimary}"/>
                    <TextBlock Text="Force-reprovision every agent sandbox image. Use this if startup keeps failing to auto-provision."
                               FontSize="12" Foreground="{DynamicResource TextMuted}" TextWrapping="Wrap"/>
                </StackPanel>
                <Button Grid.Column="1" Content="Rebuild" Classes="Secondary" Padding="15,6" VerticalAlignment="Center"
                        Command="{Binding RebuildSandboxImagesCommand}"/>
            </Grid>
        </Border>

        <!-- Add Repos to Mainguard OS: the repo-onboarding engine, DataContext rebound to AddRepos. -->
        <StackPanel Spacing="10" DataContext="{Binding AddRepos}" x:DataType="vm:AddReposToOsViewModel">
            <Grid ColumnDefinitions="Auto,*">
                <PathIcon Grid.Column="0" Data="{StaticResource FolderIcon}" Width="18" Height="18"
                          Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Grid.Column="1" Text="Copy repositories into Mainguard OS" FontSize="18" FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center" TextWrapping="Wrap"/>
            </Grid>
            <TextBlock FontSize="12" TextWrapping="Wrap" Foreground="{DynamicResource TextMuted}"
                       Text="Mainguard agents work on copies of your repositories inside the isolated Mainguard OS — your own working folders are never touched. Point Mainguard at your repos and choose which to copy. A repository that is already in Mainguard OS just succeeds again (nothing is duplicated), and any repository is also copied automatically the first time you open it."/>

            <Border Classes="Card" Background="{DynamicResource SurfaceCard}" CornerRadius="8"
                    Padding="14" IsVisible="{Binding HasRepoNotice}">
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <PathIcon Data="{StaticResource WarningIcon}" Width="16" Height="16"
                              Foreground="{DynamicResource DangerBrush}" VerticalAlignment="Top"/>
                    <TextBlock Text="{Binding RepoNotice}" FontSize="12"
                               Foreground="{DynamicResource TextPrimary}" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <StackPanel Orientation="Horizontal" Spacing="8" IsVisible="{Binding IsRepoScanning}">
                <PathIcon Classes="spinning" Data="{StaticResource RefreshIcon}" Width="14" Height="14"
                          Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Center"/>
                <TextBlock Text="Scanning for git repositories…" FontSize="12"
                           Foreground="{DynamicResource TextMuted}"/>
            </StackPanel>

            <StackPanel Spacing="10" IsVisible="{Binding IsRepoChoice}">
                <Button Classes="Primary" Padding="14,12" HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Left"
                        Command="{Binding PickRepoFolderCommand}">
                    <Grid ColumnDefinitions="Auto,*">
                        <PathIcon Grid.Column="0" Data="{StaticResource FolderIcon}" Width="18" Height="18" Margin="0,0,12,0"
                                  Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" Spacing="2">
                            <TextBlock Text="I keep my repos in one folder" FontWeight="SemiBold" FontSize="13"
                                       Foreground="{DynamicResource TextPrimary}"/>
                            <TextBlock Text="Pick that folder and Mainguard finds every git repository inside it."
                                       FontSize="11" Foreground="{DynamicResource TextMuted}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Grid>
                </Button>
                <Button Classes="Primary" Padding="14,12" HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Left"
                        Command="{Binding PickIndividualReposCommand}">
                    <Grid ColumnDefinitions="Auto,*">
                        <PathIcon Grid.Column="0" Data="{StaticResource DocumentAddIcon}" Width="18" Height="18" Margin="0,0,12,0"
                                  Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" Spacing="2">
                            <TextBlock Text="Pick individual repositories" FontWeight="SemiBold" FontSize="13"
                                       Foreground="{DynamicResource TextPrimary}"/>
                            <TextBlock Text="Select one or more repository folders yourself."
                                       FontSize="11" Foreground="{DynamicResource TextMuted}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Grid>
                </Button>
            </StackPanel>

            <ItemsControl ItemsSource="{Binding RepoRows}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="vm:OnboardRepoRowViewModel">
                        <Border Classes="Card" Background="{DynamicResource SurfaceCard}"
                                CornerRadius="8" Padding="14" Margin="0,0,0,8">
                            <Grid ColumnDefinitions="Auto,*">
                                <Panel Grid.Column="0" Width="24" Height="22" Margin="0,0,10,0" VerticalAlignment="Top">
                                    <CheckBox IsChecked="{Binding IsSelected}" IsVisible="{Binding CanSelect}"
                                              VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                    <PathIcon Classes="spinning" Data="{StaticResource RefreshIcon}" Width="16" Height="16"
                                              Foreground="{DynamicResource AccentBrush}" IsVisible="{Binding IsProvisioning}"/>
                                    <PathIcon Data="{StaticResource CheckmarkIcon}" Width="18" Height="18"
                                              Foreground="{DynamicResource SuccessBrush}" IsVisible="{Binding IsOnboarded}"/>
                                </Panel>
                                <StackPanel Grid.Column="1" Spacing="4">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14"
                                                   Foreground="{DynamicResource TextPrimary}"
                                                   TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                        <TextBlock Text="In Mainguard OS" FontSize="11" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource SuccessBrush}" VerticalAlignment="Center"
                                                   IsVisible="{Binding IsOnboarded}"/>
                                    </StackPanel>
                                    <TextBlock Classes="Mono" Text="{Binding Path}" FontSize="11"
                                               Foreground="{DynamicResource TextMuted}" TextTrimming="CharacterEllipsis"
                                               HorizontalAlignment="Left"/>
                                    <TextBlock Text="{Binding StatusMessage}" FontSize="12"
                                               Foreground="{DynamicResource TextMuted}" TextWrapping="Wrap">
                                        <TextBlock.IsVisible>
                                            <MultiBinding Converter="{x:Static BoolConverters.And}">
                                                <Binding Path="HasStatusMessage"/>
                                                <Binding Path="!IsFailed"/>
                                            </MultiBinding>
                                        </TextBlock.IsVisible>
                                    </TextBlock>
                                    <StackPanel Orientation="Horizontal" Spacing="6" IsVisible="{Binding IsFailed}">
                                        <PathIcon Data="{StaticResource WarningIcon}" Width="12" Height="12"
                                                  Foreground="{DynamicResource DangerBrush}" VerticalAlignment="Top"
                                                  Margin="0,2,0,0"/>
                                        <TextBlock Text="{Binding StatusMessage}" FontSize="12"
                                                   Foreground="{DynamicResource DangerBrush}" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Grid ColumnDefinitions="*,Auto">
                <TextBlock Grid.Column="0" FontSize="11" Foreground="{DynamicResource TextMuted}"
                           VerticalAlignment="Center" TextWrapping="Wrap" Margin="0,0,10,0"
                           IsVisible="{Binding IsProvisioningRepos}"
                           Text="Copying into Mainguard OS. You can cancel anytime — repositories already copied stay available."/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="10">
                    <Button Content="Cancel copy" Classes="Secondary" Padding="15,6"
                            IsVisible="{Binding IsProvisioningRepos}"
                            Command="{Binding CancelRepoCopyCommand}"/>
                    <Button Content="Choose again" Classes="Secondary" Padding="15,6"
                            IsVisible="{Binding ShowRepoChooseAgain}"
                            Command="{Binding ChooseReposAgainCommand}"/>
                    <Button Content="Copy selected to Mainguard OS" Classes="Primary" Padding="15,6"
                            IsVisible="{Binding ShowCopyReposPrimary}"
                            Command="{Binding CopySelectedReposCommand}"/>
                    <Button Content="Copy selected to Mainguard OS" Classes="Accent" Padding="15,6"
                            IsVisible="{Binding ShowCopyReposAccent}"
                            Command="{Binding CopySelectedReposCommand}"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </StackPanel>
</UserControl>
