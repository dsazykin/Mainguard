using Avalonia.Controls;

namespace Mainguard.Agents.UI.Views;

/// <summary>Settings → Agent Jails: a page in the shell's Settings window (a UserControl, resolved by
/// ViewLocator), like PR Intake.</summary>
public partial class JailLimitsSettingsView : UserControl
{
    public JailLimitsSettingsView()
    {
        InitializeComponent();
    }
}
