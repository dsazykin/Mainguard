using Avalonia.Controls;

namespace Mainguard.Agents.UI.Views;

/// <summary>Settings → Agent Defaults: a page in the shell's Settings window (a UserControl, resolved by
/// ViewLocator), like Agent Jails and PR Intake.</summary>
public partial class AgentDefaultsSettingsView : UserControl
{
    public AgentDefaultsSettingsView()
    {
        InitializeComponent();
    }
}
