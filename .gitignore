# Gemini CLI internal session files
.antigravitycli/
.agents/
.claude/
.impeccable/
.github/skills/

# .NET Build Outputs
[Bb]in/
[Oo]bj/
[Pp]ublish/
.avalonia-build-tasks/

# User-specific and User Preferences
*.user
*.userprefs
*.suo
*.sln.docstates
*.VisualState.xml

# Visual Studio / VS Code configuration caches
.vs/
.vscode/
!.vscode/launch.json
!.vscode/tasks.json

# Rider / JetBrains IDEs
.idea/
*.sln.DotSettings.user

# Local SQLite Databases and Write-Ahead Logs
*.db
*.db-journal
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Operating System Metadata
.DS_Store
Thumbs.db
[Dd]esktop.ini

# NuGet Packages & Restores
*.nupkg
packages/

# Shared Session Blackboard
.session_map.json
.cortex_plan.md

# Environment variables
.env
.cloudflare.env
Agentic_IDE_Implementation_Plan.md

# Headless render harness output (not committed)
artifacts_headless/

# Packaging output: build/velopack/pack.ps1's publish + RELEASES feeds, and the MG-15
# elevated-components stage the Pro head self-contained-publishes at Publish time (~90 MB).
artifacts/

# MainguardOS payload build products (built by build/mainguardos/build.sh; never committed)
build/mainguardos/out/
build/mainguardos/payload/

# P2-18 pinned libvterm build output (built by build/libvterm/build.sh; never committed)
build/libvterm/out/

# Sandbox jail image tars (docker save output from CI ci.yml sandbox-security; hundreds of MB, never
# committed — bundled beside the app at package time, warn-if-missing)
build/images/

# Website (site/)
.cloudflare.env
site/node_modules/
site/dist/
site/worker/node_modules/
site/worker/.wrangler/
